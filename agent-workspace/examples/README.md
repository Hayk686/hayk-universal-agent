# Examples

Use this folder for **non-sensitive**, **fictional** samples (tiny text/markdown only).

Do **not** add real customer files, Excel workbooks, credentials, or dumps. The main repo stays source-only; production data stays on the Pi.

## Ideas

- A minimal `.txt` fixture showing expected `input/` shape  
- A stub markdown report matching your `reports/` naming convention
